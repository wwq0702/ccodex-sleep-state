# 从 GPT-Load 学什么，哪些不搬过来

我们要解决的是：用户下载后能接上自己的 Codex，遇到问题知道卡在哪一步，退出后能回到原来的配置。不是把一个本地小工具变成需要维护账号池和数据库的中转平台。

这份对照覆盖本次查看到的主要功能域，不代表「GPT-Load 的全部功能已经移植」。标为待做的功能，不应出现在发布说明的完成列表里；新代码的测试结果以 [测试说明](testing.md) 为准。

## 参考版本与许可证

本次只读参考 [DesuwaDev/gpt-load](https://github.com/DesuwaDev/gpt-load)，固定提交 [a4f9f3606b6edf7cd0f1ad6c40b71c3735632804](https://github.com/DesuwaDev/gpt-load/tree/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804)，核对了 README、管理 API 路由、调度、健康、模型归因和部署代码。

上游项目本体使用 [MIT License](https://github.com/DesuwaDev/gpt-load/blob/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804/LICENSE)。其依赖另有各自许可证，不能只看仓库首页的 MIT 就把整个依赖树当成 MIT。本轮借鉴职责划分和交互思路，不复制它的网关实现、界面或指纹库；本项目继续使用原有 GPL-3.0 许可。如果以后引入代码或数据，需要一并保留对应版权及许可证。

## 先把几个容易混淆的词说清楚

- **我们的订阅**通常是代理节点订阅；GPT-Load 的订阅渠道则指 Codex、Claude 等服务的账号授权。不是同一回事。
- **Team 账号识别**与 **API 多凭据池**也不是一回事。这里识别账号类型是为了选择 state 筛选规则，不意味着要导入、轮换整个团队的登录凭据。
- GPT-Load 的 `degradation` 模块使用 ModelTrace 数字采样指纹库。它的 `292–332` 是请求模型输出的数字数量范围，**不是 turn-state 长度规则的证据**。统计归因也不能作为模型身份或「降智」的确定证明。[对应算法与数据来源](https://github.com/DesuwaDev/gpt-load/tree/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804/internal/degradation)
- 「一键配置」不等于静默覆盖已有配置。「无缝切换」不等于在生成到一半时换账号、换模型，再悄悄重放一遍问题。

## 功能对照

这里的「已有」指本项目原有可用能力；「本轮新增」只表示代码已加入，不能代替本轮验收。

### 配置、启动与管理

| GPT-Load 的做法 | 本项目的处理 | 状态 |
| --- | --- | --- |
| 单入口管理 UI | 同一个本地服务提供代理、注入、来源与路由页面 | 已有 |
| 首次启动生成管理口令 | 保留本地随机口令、回环监听、Origin/Host 校验；新增短期一次性启动凭证免复制，不把长期口令放进 URL | 原有鉴权 + 本轮新增免复制入口 |
| 三步首次配置 | 本地版缩短为双击自动接入并打开浏览器 → 重启 Codex 发短消息；首页按钮用于再次修复；复杂设置放连接页 | 本轮新增；旧会话不热切换 |
| 原生二进制启动 | `setup` 创建缺失配置，保留现有配置，启动并打开管理页 | 本轮新增 |
| Windows 桌面安装体验 | 增加 `start.cmd`；不要求用户先执行 `init` | 本轮新增；不是 Windows 服务安装器 |
| macOS 原生启动 | 增加 `start.command`，按脚本位置找程序，支持空格路径 | 本轮新增 |
| 系统服务、开机启动、更新器 | 暂不自动注册后台服务，保留可见终端和 Ctrl+C 恢复 | 未实现 |
| Docker Compose / 数据卷 | 本项目主要面向 Windows/macOS 本机 Codex，暂不新增容器部署 | 不在本轮范围 |
| SQLite / MySQL / PostgreSQL | 不引入数据库，继续用本地配置、日志和内存 state | 不移植 |
| 1.x / 2.x 升级隔离 | 借鉴「先保留旧数据再迁移」，但不能照搬上游“不支持原地迁移”的方案 | 由本项目配置修复流程处理 |
| 管理操作审计 | 现有配置保存先备份；更完整的可查询管理审计 | 部分已有，完整审计待做 |
| 多语言界面 | 优先中文说明和可理解的错误，不扩展多语言维护面 | 不在本轮范围 |

### 连接方式与协议

| GPT-Load 的做法 | 本项目的处理 | 状态 |
| --- | --- | --- |
| 官方、云厂商、自定义中转渠道 | 只处理当前 Codex 的官方 ChatGPT 与 Responses 兼容 API/中转 | 已有；不宣称支持所有厂商 |
| OAuth 授权、刷新、凭据导入 | 借用 Codex 当前请求中的凭据；不另做登录、不刷新用户登录 | 不移植账号托管 |
| API Key 与订阅凭据分离 | 官方凭据不能送到中转，API 中转不注入官方 state | 已有 |
| 多协议原生适配和转换 | 保留 Responses HTTP/SSE，不自动转换 Anthropic/Gemini/Chat Completions | 未实现其他协议 |
| Images / Embeddings / Rerank | 与本地 Codex state 任务无关，不增加这些路由 | 不在本轮范围 |
| Responses WebSocket | 继续明确关闭，不把没有端到端验收的能力写成支持 | 未实现 |
| Responses 持久续接归属 | 不引入多账号切换；不跨账号、跨模型复用 state | 原有绑定机制，隔离规则持续完善 |
| 远程压缩能力声明 | 保留官方能力，分别处理 V1/V2，不靠改展示名冒充能力 | 本轮模拟 V1/V2 通过；真实 Sol V2 与后续回复通过，V1 直连 404、桥接另验 |
| 分层代理设置 | 已支持 HTTP/SOCKS、本地文件、远程订阅和节点筛选 | 已有；不等于上游的账号/组/全局代理层级 |
| 多模型目录、模型发现 | Astra、Sol、Terra 的模型与 state 独立；不把任意输入映射成另一模型 | 包级回归通过；真实 Sol/Terra 普通回复通过，Astra 本轮失败；完整发现待做 |
| 模型价格同步 | 不同步价格，不把套餐额度估算成 API 账单 | 未实现 |

### 路由、健康与失败处理

| GPT-Load 的做法 | 本项目的处理 | 状态 |
| --- | --- | --- |
| 路由预检、可用性原因码 | 借鉴「为什么不能走」的解释，不只返回一个 503 | 基础已有，诊断继续补齐 |
| 软会话亲和 | state 与出口绑定；不会为了随机均衡把 state 带去别的出口 | 已有 |
| 分组/凭据权重调度 | 本地单账号不需要账号池权重；当前是出口选择，不是账号分配 | 未实现权重调度 |
| 模型级冷却 / 凭据级健康 | 需要区分网络失败、认证拒绝、限流和 state 不匹配 | 已有部分停止规则，细分诊断继续补齐 |
| 可配置重试及失败切换 | 采集有次数和冷却上限；新增受限手动再采集和无 state 普通转发兜底；用户生成不重放 | 原有停止规则 + 本轮新增操作 |
| 401 / 403 / 429 处理 | 停止，不通过换出口、清除状态或换凭据继续请求 | 已有，不能为了“无缝”破坏这条规则 |
| RPM / 并发限额 | 现有采集串行和每轮预算；完整可配置业务并发限制 | 部分已有，完整限额待做 |
| AccessKey 权限、额度、到期 | 本地程序不是多人 API 分销网关，不新增发卡与计费体系 | 不移植 |
| 调度 checkpoint | 不把账号 state 持久化，重启后重新获取 | 不移植账号状态落盘 |
| 错误来源与副作用分离 | 借鉴健康判定结构：解释错误不等于触发重试 | 设计原则，逐步落实 |

### 排错、记录与质量观察

| GPT-Load 的做法 | 本项目的处理 | 状态 |
| --- | --- | --- |
| 请求日志与过滤 | 保留元数据日志，不记录聊天正文、完整 state 和认证信息 | 已有；完整查询过滤待做 |
| 用量、缓存、费用统计 | 不能凭请求数推算套餐剩余额度；精确上游 usage 解析另做 | 待做 |
| 凭据健康与额度观察 | 本地只显示当前会话的认证/限流状态，不读取全账号池 | 已有部分状态 |
| 模型归因检测 | 可以作为另一个手动、计费前确认的诊断工具，不自动后台采样 | 未引入指纹库或检测器 |
| 降智监控与定时任务 | 不能靠 state 长度宣告质量好坏，也不自动增加模型请求 | 未实现 |
| 支持资料导出 | 新增 `doctor` 白名单摘要；不原样转储管理接口与错误正文 | 本轮新增 |
| 导入导出、批量管理 | 代理来源已有输入；完整配置迁移包还需要加密和凭据排除策略 | 部分已有，迁移包待做 |
| 重置卡/套餐重置 | 不集成自动兑换，不在失败修复里调用 | 明确不做自动兑换 |

功能来源除了 README，还核对了 [管理端路由表](https://github.com/DesuwaDev/gpt-load/blob/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804/internal/control/http_routes.go)、[路由诊断](https://github.com/DesuwaDev/gpt-load/blob/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804/internal/scheduler/inspect.go) 和 [错误决策结构](https://github.com/DesuwaDev/gpt-load/blob/a4f9f3606b6edf7cd0f1ad6c40b71c3735632804/internal/health/decision.go)。这些链接固定到提交，不会随上游更新悄悄改变。

## 先把本地用户的这条路走通

1. **启动不用猜。** 双击启动脚本，或执行 `ccodex-sleep-state setup`，自动接入并打开浏览器，首页按钮用于再次修复。已有订阅、代理和设置不会被随意重置。若服务 JSON 损坏，会进入只读修复面板：先展示操作，再由用户确认备份并创建默认配置，修复后重新启动。脚本不是安装器，不注册系统服务。
2. **接管结果说清楚。** 区分服务已启动、配置已接管、请求已经过、已有可用 state。四者不能混成一个绿色开关。
3. **切 CCS 不丢配置。** 改了连接或认证时暂停接管，保留双方文件；只改注释等无关内容不应造成整个服务不可用。修复前做备份，冲突由用户明确选择。
4. **采不到就解释。** 账号规则、长度不匹配、网络、未完整回复、冷却和上游拒绝分别显示。Team 的 332 不该再按个人 292 一概拒绝；312/356 也不能改个数字冒充合格。
5. **不会排错也有出口。** 执行 `ccodex-sleep-state doctor`，得到只读摘要。使用这份摘要反馈问题，而不是发送自己的配置、订阅链接和登录文件。

启动和 `doctor` 本身不发送模型请求。接管后，用户在 Codex 中开始对话，仍可能触发有预算的 state 采集。初次安装不会自动做真实模型连通性测试。

## 一键启动的验收边界

- 首次配置、安全重复运行、无效配置保留、符号链接拒绝、脱敏报告都有独立测试。
- `--no-config` 适用于 `serve` 和 `setup`，仍然表示不修改 Codex 配置，不会被“一键”绕过。
- 启动脚本需要和对应平台二进制一起打包；源码 checkout 中没有编译产物时会明确提示，不会偷偷下载程序。
- macOS 的系统安全提示需要用户正常授权；脚本不清除隔离属性，不关闭 Gatekeeper。
- 双击 Windows 脚本的实际体验、CCS 不同版本、各中转站、真实 Team 账号与压缩协议都需要各自验收。Sol V2 真实成功不能替代 V1；本轮 V1 直连失败已经记入问题清单。通过单元测试不是“所有环境永远兼容”的证明。
