# 提交改动

小改动先写清楚要解决什么，再给测试。不要为了加一个协议，把系统代理控制、账号管理或订阅数据库一起搬进来。

几条不太能商量的边界：

- 测试只能用合成凭据和模拟上游，不把个人环境做成 fixture。
- 业务日志不能出现请求正文、认证、完整 state、订阅 URL、节点名称或代理密码。
- state 必须按凭据和模型隔离，并记录采集出口；响应观测不能直接覆盖 active。仅用户明确开启独立转发出口时允许跨出口实验，不把节点身份当作公网 IP 身份。
- 不自动重放生成请求，不在 401/403/429 后继续换节点尝试。
- 任何 Codex 配置修改都要有独立备份、恢复记录和冲突保护。
- 新协议或新平台要分清“能解析”“能编译”和“实际跑通”，不要混写成已支持。

提交前运行 `go vet ./...`、`go test -race ./...`，检查 Windows 路径、换行和文件锁。涉及文案时，给用户写下一步能做什么，少写宏大的效果承诺。

没有对应测试的“清理代码”容易把稳定性清理掉。改状态机时请同时覆盖过期、并发、旧响应和取消；改配置写入时覆盖崩溃与用户中途编辑。

## 作者与贡献记录

上游项目维护者为 Ethan（GitHub：gylive）；本仓库为 wwq0702 维护的 Fork。维护权、提交作者和 GitHub Contributors 列表不是同一个概念：接受别人的补丁，应保留真实的人类作者与提交来源，不把贡献改写成维护者独自完成。

后续提交不自动添加 AI 工具的 `Co-Authored-By`。作者身份应对应实际提交者；可以在 PR 说明中如实写使用了哪些辅助工具，不制造不存在的人类或工具贡献。

历史 PR #2 的提交 `cc54b25` 包含 Claude 的共同作者尾注，因此 GitHub 可能显示额外贡献者。这是历史元数据，不代表 Claude 是项目维护者。本轮不会为修改侧栏而重写公开历史、强推或把真实贡献映射成维护者。

## 升级协作与验收

升级先在功能分支测试与构建，维护者明确授权且验收通过后才能合并和发布。功能修改须有对应回归，真实外联测试要有明确授权、有限请求预算与失败停止规则。`apikey.txt`、原始响应及个人环境只放忽略的私有目录，绝不提交到仓库。

验收描述必须区分模型列表、连接、生成、state 采集与压缩；`/models` 成功不写成真实生成通过。界面应把下一步操作放前面，把有额度或配置写入影响的操作说明白。合规边界见[使用说明](docs/compliance.md)，不把改名或免责声明当作规避义务的方法。
